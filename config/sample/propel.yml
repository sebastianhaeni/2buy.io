propel:
    database:
        connections:
            default:
                adapter: sqlite
                dsn: sqlite:api/2buy.sq3
                user: root
                password:
                settings:
                    charset: utf8
