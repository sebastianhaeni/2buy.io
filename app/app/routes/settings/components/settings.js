import React from 'react';
import { DocumentTitle } from 'lib/components';

export default React.createClass({

  render() {
    return (
      <DocumentTitle title="Settings">
        <p>There are currently no settings. Work in progress.</p>
      </DocumentTitle>
    );
  }

});
